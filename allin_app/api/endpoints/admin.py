# Placeholder for Admin REST endpoints (Phase 4)
from fastapi import APIRouter

router = APIRouter(prefix="/admin", tags=["Admin"])

# Example endpoints (to be implemented):
# /sync_knowledge
# /system-prompt
